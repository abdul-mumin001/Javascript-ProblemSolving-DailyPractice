//Q. Given a strings, return all the characters of string separated by comma (,)

// Input:
// aircampus
// Output:
// a,i,r,c,a,m,p,u,s

function commaSep(str) {
  return str.split("").join();
}
console.log(commaSep("Javascript"));
console.log(commaSep("AbdulMumin"));
